services:
  backend:
    build:
      context: ./backend
    ports: 
      - '3000:3000'
    volumes: 
      - ./backend:/docker-app
      - /docker-app/node_modules
    command:
      - nodemon 
      - -L
      - server.js